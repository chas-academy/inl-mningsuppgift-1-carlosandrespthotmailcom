#include <stdio.h>
#include <stdlib.h>

int main()
{
    // The Line below must *NOT* be changed or test will fail!
    srand(1);

        int numbers[100];

        int dice_roll = (rand() %6) +1 ;
        
        for(int i = 0; i <100; i++ )

    
    // Your code goes here

    return 0;
}
